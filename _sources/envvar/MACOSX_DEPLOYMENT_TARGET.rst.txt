MACOSX_DEPLOYMENT_TARGET
------------------------

.. include:: ENV_VAR.txt

指定要部署目标二进制文件的macOS的最低版本。

The ``MACOSX_DEPLOYMENT_TARGET`` environment variable sets the default value for
the :variable:`CMAKE_OSX_DEPLOYMENT_TARGET` variable.
