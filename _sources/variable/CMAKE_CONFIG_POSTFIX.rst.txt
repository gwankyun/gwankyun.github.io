CMAKE_<CONFIG>_POSTFIX
----------------------

配置\ ``<CONFIG>``\ 下的库的默认文件名后缀。

When a non-executable target is created its :prop_tgt:`<CONFIG>_POSTFIX`
target property is initialized with the value of this variable if it is set.
